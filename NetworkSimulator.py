
def example_step(global_time: float):
    # This is called by the simulator at every tick, and the global_time is passed in as an argument
    # In here the object advances it's internal state
    # Acknowledge any recived packets, save their time state and compare with global time if anything has to be sent
    pass

